# USAI — Frontend

SPA em React + Vite + TypeScript + Tailwind CSS. Consome a API em `apps/backend`.

```bash
npm install
npm run dev      # http://localhost:5173
npm test         # Vitest + Testing Library
npm run build
```

Veja a visão geral do projeto no [README da raiz](../../README.md).
